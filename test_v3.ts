import fetch from "node-fetch"; import FormData from "form-data"; import fs from "fs"; async function run(){ const form = new FormData(); form.append("encoded_image", fs.createReadStream("package.json"), { filename: "snip.jpg", contentType: "image/jpeg" }); const res = await fetch("https://lens.google.com/v3/upload", { method: "POST", body: form, redirect: "manual", headers: { ...form.getHeaders(), "User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7) Chrome/120.0.0.0 Safari/537.36" } }); console.log(res.headers.get("location")); } run();
